import React from "react";
import * as Styled from "../../styles/AdminAuth";

const AuthSubmitButton = () => {
  return <Styled.AdminAuthButton type="submit">Submit</Styled.AdminAuthButton>;
};

export default AuthSubmitButton;
